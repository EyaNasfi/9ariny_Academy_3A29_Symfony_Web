<form method="post" action="{{ path('app_remise_delete', {'id': remise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ remise.id) }}">
    <button class="btn">Delete</button>
</form>
